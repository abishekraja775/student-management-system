package com.example.studentmanagement;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic test to verify the Spring application context loads correctly.
 * Run this to confirm your project is set up properly.
 */
@SpringBootTest
class StudentManagementApplicationTests {

    @Test
    void contextLoads() {
        // This test passes if the Spring application context loads without errors
        System.out.println("✅ Spring Boot context loaded successfully!");
    }
}
